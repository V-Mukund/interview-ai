{
  "name": "interview-ai-monolith",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:all": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npx concurrently \"npm run start:prod --prefix backend\" \"npm run start --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
